<div class="admin-all-purchases-container">  
  <div class="board-title">
    <p>Tổng đơn nhập: {{totalPurchase}}</p>
  </div>

  <div class="filter-and-create">
    <div class="sort">
      <label for="price">Sắp xếp theo:</label>
      <select id="price" class="filter-btn">
        <option value="">Giá mặc định</option>
        <option value="1">Giá tăng dần</option>
        <option value="-1">Giá giảm dần</option>
      </select>

      <select id="rate" class="filter-btn">
        <option value="">Đánh giá mặc định</option>
        <option value="1">Đánh giá tăng dần</option>
        <option value="-1">Đánh giá giảm dần</option>
      </select>

      <select id="saleNumber" class="filter-btn">
        <option value="">Lượng bán mặc định</option>
        <option value="1">Lượng bán tăng dần</option>
        <option value="-1">Lượng bán giảm dần</option>
      </select>

      <button id="clear-filter" style="display: none">Xoá bộ lọc</button>
    </div>

    <a href="/admin/all-purchases/purchase/create" class="create-btn" id="create-btn">
      <i class="fi fi-rr-plus"></i>
      <p>Thêm Đơn Nhập</p>
    </a>
  </div>

  <div class="table">
    <input type="text" id="search-input" placeholder="Tìm kiếm khách hàng">
    <table class="all-items">
      <thead>
        <tr>
          <td style="width:  5%">STT          </td>
          <td style="width: 20%">Mã đơn nhập  </td>
          <td style="width: 25%">Ngày nhập    </td>
          <td style="width: 15%">Tổng sản phẩm</td>
          <td style="width: 25%">Tổng chi phí </td>
          <td style="width: 10%">Chi tiết     </td>
          </td>
        </tr>
      </thead>

      <tbody>
        {{#each purchases}}
        <tr>
          <td>{{addIndex @index 1}}</td>
          <td>{{this._id}}</td>
          <td>{{formatDate this.purchaseDate}}</td>
          <td>{{this.totalProducts}}</td>
          <td>{{formatNumber this.totalPurchasePrice}}</td>
          <td><a href="/admin/all-purchases/purchase/{{this._id}}">Xem</a></td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>

  <span class="pagination">
  </span>
</div>

<script>
  const successful = '{{successful}}'
  const totalPurchase = '{{totalPurchase}}'
  const currentPage = '{{currentPage}}'
  loadFile('/js/admin/all/purchase.js')
</script>